# 桌面便签 (Desktop Note)

Windows 桌面便签应用，支持多个便签、快捷键显示/隐藏、窗口置顶。

## 功能特性

- **多便签管理**: 创建、切换、删除多个便签
- **快捷键控制**: 全局快捷键显示/隐藏便签 (默认 `Ctrl+` `)
- **窗口置顶**: 可切换窗口置顶状态
- **透明背景**: 支持透明度调节
- **数据持久化**: 便签内容自动保存到本地

## 数据存储位置

便签数据存储在以下文件：

- `notes.json` - 便签内容（ID、内容、位置、透明度等）
- `window-config.json` - 窗口配置（尺寸、位置、默认快捷键、透明度）

存储路径: `C:\Users\<用户名>\AppData\Local\DesktopNote` (Windows)

## 安装与运行

```bash
# 安装依赖
npm install

# 开发模式运行
npm run dev

# 构建生产版本
npm run build

# 启动应用
npm start
```

## 使用说明

### 基础操作

- **新建便签**: 点击底部「+ 新建」按钮
- **切换便签**: 点击顶部便签标签
- **删除便签**: 点击底部「×」关闭按钮 (最后一个便签会清空内容)

### 窗口控制

- 顶部按钮: 最小化、最大化、关闭
- 底部「📌」按钮: 切换窗口置顶

### 设置

点击底部「⚙」打开设置面板：

- **透明度**: 调节便签背景透明度
- **快捷键**: 点击按钮后按下新快捷键

## 技术栈

- React + Tailwind CSS
- Vite
- Electron

## 项目结构

```
├── src/           # React 源码
├── main.cjs      # Electron 主进程
├── preload.cjs   # 预加载脚本
├── vite.config.js
└── tailwind.config.js
```
